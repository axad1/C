#include<stdio.h>
int main()
{
/*	
	The output of bitwise AND is 1 if the corresponding bits of two operands is 1.
	If either bit of an operand is 0, the result of corresponding bit is evaluated to 0.
*/
    int a = 12, b = 25;
    printf("AND Operation = %d", a&b);
}
